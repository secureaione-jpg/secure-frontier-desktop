// Minimal preload. Kept intentionally empty for now — context isolation is on
// and the app simply renders the live site. Add a bridged API here later if the
// desktop app ever needs native capabilities (file access, notifications, etc.).
